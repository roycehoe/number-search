version: '3.9'
services:
  database:
    image: mongo
    restart: always

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    depends_on:
      - database
    volumes:
      - /app/node_modules
      - ./backend:/app
    ports:
      - "3000:3000"